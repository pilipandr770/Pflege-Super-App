# ⚠ Diese Datei ist veraltet.
# Das kanonische docker-compose.yml liegt im Repo-Root:
#
#   cd ..        (eine Ebene nach oben)
#   make up      (oder: docker compose up)
#
# Weitere Befehle: make help
